import styled from 'styled-components';
import palette from '../../palette/palette';

const HeaderUserProfileStyle = styled.div`
  .header-user-profile-wrapper {
    border: 2px solid #48cfae;
    color: ${palette.black};
    background-color: white;
    right: 40px;
  }
`;

export default HeaderUserProfileStyle;
